public class VendingMachine {

    //add new products, profit on a date range, upi, card transactions, maintain stock

    //Product - id, name, price
    //Stock - map of products with their number
    //Order - product_id, date, modeOfTxn, totalPrice, txnType, txnId
    //TransactionMode - type, upiId, card number

    //find total sales  product id 1 between 5 to 10 dec

//    select totalPrice, sum(totalPrice) as totalSales from order o
//    group by o.product_id
//    where o.product_id = 1
//    and date > 5 dec and date < 10 dec


}
