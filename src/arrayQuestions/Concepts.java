package arrayQuestions;

public class Concepts {

    /**
     * when an array is passed as a parameter in a function,
     * although java is pass by value always, the value in
     * this case is the reference of that array, so the changes
     * happen in that same array, and this is called shallow copy.
     * But if we create a new array using this array and copy its values,
     * then changes will not happen in this same array but the new one created.
     * This is called deep copy.
     * **/
}
